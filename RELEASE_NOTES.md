# v3.0.0 Windows 非激活窗口圆角区域刷新修复

- 根据实机复现条件重新定位：直角残边只在窗口失去焦点、切换到其他应用后出现，拖动字体管理器窗口触发完整重绘后立即消失，属于 Windows 非激活透明窗口的 DWM/Chromium 合成快照未重新应用圆角区域。
- Windows 主窗口显式启用 Electron 原生 `roundedCorners`，并新增原生窗口区域运行时，使用 `BrowserWindow.setShape()` 将 `18px` 圆角直接落实到系统可绘制与可交互区域；即使 Chromium 非激活合成层暂时出现矩形快照，圆角外侧也不允许绘制像素。
- 窗口在 `blur`、`focus`、显示、恢复、取消最大化及离开全屏时重新提交原生圆角区域，并在 DWM 状态稳定后执行第二次刷新；同时调用 `webContents.invalidate()` 请求完整重绘。
- 最大化和全屏状态自动恢复矩形区域，返回普通窗口后重新应用圆角；窗口缩放期间按尺寸更新形状，关闭时完整释放监听和定时器。
- 扩展现有圆角防回退诊断，覆盖非激活/激活切换、原生区域、完整重绘和最大化状态；版本号保持 `3.0.0`，无依赖、数据库或 Rust 变更。

# v3.0.0 Windows 圆角外侧矩形合成层修复

- 根据实机截图重新定位：圆角外侧的浅色直角块并非 DWM 阴影，而是顶栏 `backdrop-filter` 在透明 Windows BrowserWindow 中被提升为矩形离屏合成层后越过父级圆角裁切。
- 顶栏外缘改用独立的不透明渐变背景，不再在窗口边界使用 `backdrop-filter`；内部弹窗、详情面板等不接触窗口外缘的玻璃效果保持不变。
- 深色与浅色主题分别提供稳定的顶栏背景变量，保留原有层次、描边和窗口拖拽区域，同时避免缩放、跨 DPI、移动或 GPU 重绘时再次露出直角块。
- 扩展现有圆角防回退诊断，明确禁止顶栏外缘重新启用 backdrop blur；版本号保持 `3.0.0`，无依赖、数据库或 Rust 变更。

# v3.0.0 Windows 透明窗口圆角边缘修复

- Windows 透明无边框窗口不再启用 DWM 原生矩形阴影，避免圆角外侧在缩放、最大化还原或 GPU 合成刷新时露出直角边。
- 应用圆角描边由固定合成层改为应用内部绝对层，确保描边始终受 `18px` 圆角裁切。
- 顶栏背景增加同半径圆角和 `background-clip`，防止独立毛玻璃合成层在窗口顶角外溢。
- 新增无边框窗口圆角防回退诊断；版本号保持 `3.0.0`，无依赖和数据库变更。

# Release Notes

## v3.0.0 Windows 诊断构建门禁兼容性修复

- 修复 Windows + Node 22 环境中统一诊断运行器直接 `spawnSync('npm.cmd')` 返回 `EINVAL`，导致 `npm run build:win` 在第一项诊断尚未执行前中止的问题。
- `diagnostics:all` 现在优先读取 npm 注入的 `npm_execpath`，通过当前 `node.exe` 直接执行 `npm-cli.js`，不再依赖 Windows 对 `.cmd` 文件的直接子进程启动支持。
- 直接运行诊断脚本且缺少 `npm_execpath` 时，Windows 保留 shell 兼容回退；Linux/macOS 继续使用原有 npm 调用。
- 扩展现有发布构建门禁诊断，确保统一诊断运行器持续采用跨平台 npm 启动方式，避免后续改动重新引入 `npm.cmd EINVAL`。
- 未新增依赖、未修改业务代码、未修改 Rust 源码、未更新版本号。

## v3.0.0 本地标签目录与字体绑定分离修复

- 本地标签现在明确分为“标签目录”和“字体绑定”两层：`app_state.localTags` 保存长期存在的标签目录，`local_font_tags` 只保存字体与标签之间的绑定关系。
- 从字体上移除最后一个本地标签绑定时，只删除该字体绑定；标签目录条目继续保留，并以数量 `0` 显示在侧边栏。
- 只有用户主动执行“删除标签”时，才会从标签目录移除该标签，并同步删除所有字体绑定。
- Rust 与 Node 兼容路径都会将既有标签目录、当前绑定标签和本次新增标签合并保存；普通解绑不再通过绑定表反推并覆盖标签目录。
- Rust 本地标签读取会合并标签目录与历史绑定，用于兼容旧数据库；标签签名同时包含目录内容，空标签创建、保留或显式删除都会正确触发查询失效。
- 日志新增 `local known tag retained empty`，用于确认最后一个字体解绑后标签仍被保留；显式删除标签记录为 `local known tag deleted`，不再把普通解绑记录为零绑定删除。
- 渲染端继续使用后端 `knownTags` 作为标签目录权威集合：空标签保留在侧边栏，旧分页不能恢复已显式删除的标签。
- 已经被旧版本错误自动删除的空标签名称无法从绑定表恢复，需要重新创建一次；应用本修复后会一直保留，直到用户主动删除。
- 扩展既有标签一致性诊断，覆盖最后解绑保留空标签、显式删除空标签、Rust/Node 标签目录持久化和日志语义；未新增依赖、未修改数据库结构、未更新版本号。

## v3.0.0 本地标签显式删除前端一致性

- 用户主动删除本地标签后，标签状态确认事件会先失效分页与指标请求，再异步保存 Library shell；删除前启动的旧请求不能把已删除标签重新写回界面。
- LibraryState 的标签权威标记只用于约束后端已确认的标签目录：目录中存在的空标签会继续显示，目录中已显式删除的标签会从旧分页、旧指标和旧字体对象中清理。
- 标签状态信号去重允许带完整 `knownTags` 的增强信号覆盖字段不完整的重复信号，确保显式删除结果不会丢失。

## v3.0.0 文件夹数量路径一致性与实测日志修复

- 修复 Windows 物理文件夹树经过 Rust `canonicalize()` 后泄漏 `\\?\` 设备路径的问题；根目录、子目录、父目录及字体路径统一按普通盘符或 UNC 路径比较，避免索引更新后总数正常但各文件夹数量为 0。
- Rust 指标路径规范化现在会正确去除 `\\?\` 与 `\\?\UNC\` 前缀，并保留 UNC 根目录的双反斜杠，不再把网络路径错误压缩成单反斜杠。
- 渲染端和 Library SQLite 加入旧设备路径迁移，已有 `folder_nodes` 数据会在读取和下一次保存时自动修复，不要求用户删除数据库或重新添加文件夹。
- 字体指标增加根目录数量完整性校验；若 Rust 指标中的根目录合计与字体总数不一致，会使用现有权威回退快照只修复 `folderCounts`，避免异常结果直接进入界面。
- 指标日志增加文件夹键数、非零键数和根目录合计，便于后续日志直接判断路径匹配是否完整。
- 文件夹选择对话框、添加文件夹和全量扫描入口改为单飞执行，快速重复点击不会同时创建多个系统目录选择窗口。
- 已安装字体按系统字体族生成预览失败时，会继续尝试同一 Rust 渲染器的字体文件路径模式，不再直接返回空预览或要求启用 Node/PowerShell 兼容回退。
- 首次尚未生成共享根索引时，启动维护将其识别为“未初始化”而不是数据库损坏，不再产生 `startup database maintenance ok=false` 的误报。
- 扫描早期可见占位字体限制为首批 300 个，仍能快速显示扫描结果，同时避免同一轮 1000+ 字体被占位和完整元数据各推送一次造成 React 更新压力。
- 新增文件夹数量路径一致性与实测日志问题防回退诊断；未新增依赖、未修改数据库结构、未更新版本号。

## v3.0.0 退出、日志与数据库维护耐久性优化

- 应用退出改为两阶段协调：先等待所有渲染窗口冲刷字体元数据写队列和 Library 配置并真正关闭，再停止后台调度、文件监控、数据库 Worker 与 Rust Core；菜单退出、安装器退出和系统 `app.quit()` 均遵循同一顺序。
- 渲染端确认仍有数据未保存并选择返回软件时，退出流程会立即终止，后台任务和窗口保持可用，不再出现窗口仍在但监控、调度或 Rust 服务已经停止的半失效状态。
- 安装状态保存队列并入主退出协调器，退出前在 Rust 和数据库服务仍可用时完成最终冲刷；移除重复的独立 `before-quit` 监听，避免多个退出处理器互相提前放行。
- 启动日志异步写入改为串行尾队列，`flushAsync()` 会排空活动写入期间追加的所有日志；崩溃和正常退出均保证关键尾日志尽量落盘。
- 增加启动会话清洁标记：启动时记录未完成状态，正常退出后标记清洁；下一次启动会明确记录上次是否异常终止，便于实测日志定位强退、崩溃或断电。
- 性能聚合日志会在停止监控后、日志最终排空前强制提交，避免退出前最后一段扫描、数据库和渲染性能数据缺失。
- 数据库备份、健康检查、预览维护和恢复操作进入统一串行队列，避免维护任务同时操作同一 SQLite 及其 WAL/SHM 文件。
- 自动备份只有在备份真正成功后才更新时间戳；失败后仍保持可重试资格，不再因一次失败而跳过后续 24 小时。
- 手动恢复会先验证备份文件完整有效，再隔离当前数据库并执行替换；无有效备份时不会提前移动或破坏当前数据库。
- 数据库备份文件名使用毫秒级时间戳，避免同一秒内多次维护发生文件名碰撞。
- 新增退出日志耐久性、数据库维护串行化和退出前渲染落盘顺序诊断；未新增依赖、未修改数据库结构、未更新版本号。

## v3.0.0 扫描生命周期与预览缓存并发可靠性优化

- 连续发起索引更新时，新任务会先取消旧任务，并等待旧扫描完整结束、释放共享索引锁和清理增量推送后再启动；排队期间被后续请求替代的扫描不会重复执行。
- 取消索引扫描会立即销毁尚未发送的增量字体批次，避免旧扫描在新扫描开始后继续向前端回灌字体。
- 索引列表 Worker 在未返回完成消息前异常退出或正常提前退出都会明确失败收敛；解析 Worker 的异常、空批次和不完整批次会转为对应任务错误，不再造成扫描永久挂起。
- 解析 Worker 失败后会立即终止并释放资源，失败批次完成错误归档后再按剩余任务补充 Worker。
- Rust Core 调度缓存增加全局及命令级 generation；清缓存后旧在途任务不能重新写回缓存，也不能删除同键新任务。
- 预览请求拆分为多个批次时，会等待全部批次完成并聚合结果，不再由最先完成的批次提前结束调用方请求。
- 高压预览读取和共享元数据签名的 in-flight 清理增加 Promise 身份校验，被限流淘汰的旧任务不能删除后来的同键任务。
- 预览索引状态读取增加按键代际隔离；写入或删除索引后，旧读取结果不能重新污染状态缓存。
- Library shell 保存成功后立即失效预览缓存根目录路由；旧的在途 Library shell 读取和 NAS 根目录探测不能覆盖新的路径或可用性判断。
- 安装状态指标补偿缓存增加 generation，清理后旧任务不能重新写回过期的字体数量结果。
- 新增扫描生命周期、Rust 调度缓存和通用 in-flight 缓存生命周期诊断，并补充预览拆批聚合实际行为测试。
- 未新增依赖、未修改数据库结构、未更新版本号。

## v3.0.0 合并索引串行化与查询缓存一致性优化

- 合并索引的增量同步、整根快照同步、后台校验重建和待构建快照写入统一进入单一串行协调器，避免多个异步任务同时改写本机 `merged-index.sqlite`。
- 不同来源的索引写入严格按请求顺序执行；单个任务失败不会阻塞后续任务，真实提交后使用单调 revision 统一触发派生查询缓存失效。
- 修复旧分页查询、内存查询或字体数量请求在缓存清理后才完成，又重新写回旧结果的问题；缓存失效现在具有 generation 隔离，并保护新旧同键 in-flight 请求。
- 文件监控批次改为单飞执行，刷新期间新到达的事件会进入下一批，不会并发写同一根索引。
- 文件夹监听重启或监听根目录变化时使用 watcher generation 隔离，旧监听任务完成后不再同步合并索引或向渲染进程回灌旧字体。
- 文件监控索引提交后会立即刷新渲染端数据库分页、筛选结果和文件夹统计，不再继续展示提交前的数据库页。
- 新增合并索引写入串行化、监听生命周期和查询缓存代际诊断，包含实际异步交错行为测试。
- 未新增依赖、未修改数据库结构、未更新版本号。

## v3.0.0 物理文件变更与根索引一致性优化

- 物理移动、批量移动、删除字体文件以及重命名物理文件夹后，会按旧路径和新路径定位所有受影响的监听根目录，并安排根目录级增量索引刷新。
- 不再只刷新重命名后的目标子目录，避免旧路径记录无法被删除、文件夹数量残留以及重启后字体回流。
- 删除字体后不再立即查询尚未更新的合并索引；等待根索引变更事件完成后再刷新数据库派生状态。
- 修复异步删除期间用户已切换字体时，旧闭包仍清空当前新选择的问题。
- 修复字体每次物理移动都会累积历史 `fontFolderIds`，导致字体长期显示在曾经经过的旧文件夹中的问题。
- 文件夹归属清理会移除与字体当前物理路径不一致的历史物理目录关系，同时保留有效虚拟目录关系。
- 新增物理文件变更与根索引一致性诊断，覆盖重叠监听根目录、部分刷新安排失败、移动历史归属清理和旧索引提前查询防护。
- 未新增依赖、未修改数据库结构、未更新版本号。

## v3.0.0 字体元数据写队列与退出一致性优化

- 修复本地标签、共享标签、收藏和删除保护在后端部分失败时，失败项被从内存队列清除且无法自动重试的问题。
- 写队列会按后端返回的失败字体 ID 精确回队；重试期间用户产生的新状态优先，旧失败快照不会覆盖新操作。
- 写队列改为返回明确的完整保存结果，关闭窗口和标签删除、共享标签重命名不会再把部分失败误判为成功。
- 每次写入使用最新文件夹列表，避免运行时闭包持有旧 NAS/本地根目录导致共享标签写入旧位置。
- 关闭冲刷超时后不再静默强制退出，主进程会显示未保存警告，由用户选择返回软件或仍然关闭。
- 安装状态异步保存批次失败后会重新入队并退避重试；保存后的合并索引同步失败不会反向误判数据库保存失败。
- 修复安装状态保存与主进程退出清理之间的双 `before-quit` 竞态，写入未结束时后续退出请求仍保持拦截。
- 新增字体元数据写队列耐久性和安装状态保存队列耐久性诊断。
- 未新增依赖、未修改数据库结构、未更新版本号。

## v3.0.0 shared tag rename latency optimization

- 优化共享标签重命名弹窗：点击保存后立即关闭，不再等待后端 IPC 完整完成。
- 新增 0 绑定共享标签重命名快速路径，避免无字体绑定标签触发 4000+ 字体索引相关刷新。
- 快速路径会写入 shared known tags，并记录 `shared known tag zero-bind renamed` 日志。
- 有绑定字体的共享标签仍保持原子重命名逻辑。
- 未改动扫描、NAS root-index、预览缓存、安装状态、普通标签主逻辑。
- 未更新版本号。

## v3.0.0 库状态持久化与退出可靠性优化

- Library shell 持久化会合并等价写入，并将队列中的旧快照提升为最新的文件夹、标签和偏好设置状态，避免旧状态覆盖新状态。
- SQLite 短暂保存失败会按有限退避策略重试；仍未成功的保存继续保留在后台恢复队列中，恢复后主动刷新数据库派生视图。
- 点击关闭、Alt+F4 或应用退出时，主进程会先要求渲染进程冲刷字体元数据写队列和库设置，再允许 BrowserWindow 关闭。
- 渲染进程确认仍有数据未保存时，应用会给出警告并允许返回软件检查，不再静默丢弃刚完成的设置或标签操作。
- 物理文件夹重命名会先保存新路径，再刷新对应文件夹索引，避免继续使用旧路径以及文件夹数量回退。
- 新增统一 `verify` 门禁，正式构建前执行 TypeScript 检查和全部已注册诊断；正式构建仍要求 Rust Core 重新编译成功。
- 增加持久化顺序、重试与合并、关闭前冲刷协议、发布验证链路的防回退诊断。
- 未新增依赖、未修改数据库结构、未更新版本号。

### 共享索引恢复与新增目录计数稳定性

- 当 `index.latest.json` 或清单中的活动数据库指针缺失、但不可变索引快照仍然存在时，自动选择最新完整快照继续读取，避免重新添加同一 NAS 根目录后无条件全量解析。
- `root.json` 缺失时从现有共享索引清单恢复 `rootId`、规范路径和别名，防止根身份变化导致机器安装状态及共享缓存失去复用。
- 新增监听目录时，在首次刷新数据库统计前读取物理文件夹树；即使共享索引尚未建立，也会立即显示各子目录及其计数位置，不再短暂只显示根目录数量。
